# Local development through the Vite proxy.
VITE_API_BASE_URL=/backend-api

# For production, use the direct API origin. Endpoint paths already include /api/v1.
# VITE_API_BASE_URL=http://jstaging.system-11.net
